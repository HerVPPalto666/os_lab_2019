# Lab1 commands (apt-based environment)

# Task 1
./background.sh &
echo "sample text" > sample.txt
cat sample.txt
clear
wc -m sample.txt

# Task 2
cat cake_rhymes.txt | grep cake > with_cake.txt
rm nonexistent.txt 2>/dev/null

# Task 3
chmod +x hello.sh
./hello.sh
chmod +x info.sh
./info.sh

# Task 4
chmod +x average.sh
od -An -tu2 -N300 /dev/random | tr -s '[:space:]' '\n' | grep -E '^[0-9]+$' | head -n 150 > numbers.txt
./average.sh $(cat numbers.txt)

# Packages (Ubuntu/apt)
sudo apt -y update
sudo apt -y install bc
